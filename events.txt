signup-verify (client)
    -userdata :: mail, name, pwd

verification-error (server)
    -[invalid mail]
    -[username already taken]
    -[email already taken]

verify-otp (server)
    -otp

verified (client)

entry-ok (server)
    -sessionId

login (client)
    -userdata :: (name, pwd) || (sessionId)

login-failed (server)
    -[invalid credentials]

auto-login-failed (server)